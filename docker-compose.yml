version: '3.8'
services:
  app:
    build: .
    ports:
      - "8085:8085"
    environment:
      - REDIS_URL=redis://redis:6379
      - PORT=8085
    depends_on:
      - redis
  redis:
    image: "redis:alpine"
    volumes:
      - redis-data:/data

volumes:
  redis-data:
